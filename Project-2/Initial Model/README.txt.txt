attrTruth.py : To create a csv file having ids of 10,000 culture images and 10,000 tag images.
BaseCustomDataLoader.py : Base Custom Dataloader to load the data in the correct format as per our requirements.
ConvNet.py : Defining various layers and parameters for the implementation of different models.
CustomDataLoader.py : Custom Dataloader to load the data in the correct format and resizing the images as per our requirements.
data_analysis_for_augmentation.py : 
labelTruth.py : 
resize.py : To resize the images as per our requirements.
train_evaluate_CNN.py : Implementation of the Initial Model along with training and testing on the dataset. Also, plots the graphs for train, test losses along with train, test accuracies.